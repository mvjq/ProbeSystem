package com.nasa.probesystem.domain.service;

public class NavigationValidationService {
  /*
  this module contains all the validation of the state of the objects
  because the domain logic is similar to the real world, is easier
  to crate rules and apply then to an state to check if is stays correct
   */
}
